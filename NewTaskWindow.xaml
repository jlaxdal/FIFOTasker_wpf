<Window x:Class="FIFOTasker_wpf.NewTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Task" Height="280" Width="460"
        WindowStyle="SingleBorderWindow" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Summary line (required)" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox x:Name="txtSummary" Height="28" MaxLength="100" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="2" Text="Detailed description (optional)" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox x:Name="txtDetails" Grid.Row="3" AcceptsReturn="True" TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto" Height="120" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add Task" Width="100" Height="32" Margin="0,0,8,0" 
                    Click="Add_Click" Background="#006400" Foreground="White"/>
            <Button Content="Never mind" Width="100" Height="32" 
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>