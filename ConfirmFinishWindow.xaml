<Window x:Class="FIFOTasker_wpf.ConfirmFinishWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Finish" Height="180" Width="380"
        WindowStyle="SingleBorderWindow" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Finish current task?" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <TextBlock x:Name="txtSummary" Grid.Row="1" 
                   TextWrapping="Wrap" TextTrimming="CharacterEllipsis" 
                   MaxHeight="80" Margin="0,0,0,24"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Yes" Width="90" Height="34" Margin="0,0,12,0" 
                    Click="Yes_Click" Background="#006400" Foreground="White" FontSize="14"/>
            <Button Content="No" Width="90" Height="34" 
                    Click="No_Click" FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>